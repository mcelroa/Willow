# Demo mode — no backend. MSW intercepts /api/* in the browser and serves
# seeded data from localStorage. Used by `npm run build:demo` / `npm run dev:demo`.
#
# Committed on purpose: this is the configuration the public demo deploy uses,
# and it contains no secrets.
VITE_DEMO_MODE=true
VITE_API_URL=/api
